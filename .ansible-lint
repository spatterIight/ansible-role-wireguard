---
profile: production # min, basic, moderate, safety, shared, production

skip_list:
  - yaml[line-length]

exclude_paths:
  - molecule/basic/prepare.yml
  - molecule/multi_tunnel/prepare.yml

offline: true
